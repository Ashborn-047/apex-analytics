## 2026-06-17 - Added ARIA labels to Modal component

**Learning:** The application's core Modal component lacked ARIA labels on its back and close buttons, which could be a common pattern across other custom components.

**Action:** Always verify icon-only buttons for missing ARIA labels in custom components, especially those used globally like Modals.
