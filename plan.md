1. **Analyze performance bottlenecks**: Review Bolt's journal which points out `useMemo` issues with mock data generators using `Math.random` and `Math.min/max` loops.
2. **Review codebase for unmemoized mock generator**: `DriverDetailModal.tsx` line 104 computes `eloProgression` on every render if `driver.history` is not present, calling `generateEloProgression(driver.elo_rating)`. `generateEloProgression` uses a loop and `Math.random()`, creating unmemoized randomized mock data that causes unnecessary layout jumps and recalculations on every render, as noted in the journal `2025-02-12 - Unmemoized randomized mock data causes UI jumps and unnecessary layout recalculations`.
3. **Implement fix in `DriverDetailModal.tsx`**: Add `useMemo` hook from `react` to `DriverDetailModal.tsx`, and wrap `eloProgression` computation with `useMemo` so it's only computed when `driver.elo_rating` changes.
4. **Pre-commit and submit**: Complete pre-commit steps. Then submit a pull request with the title `⚡ Bolt: [performance improvement]` and the required description format.
